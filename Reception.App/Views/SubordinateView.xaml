<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Reception.App.Views.SubordinateView">
  <Grid RowDefinitions="*, Auto">
    <Grid Grid.Row="0"
          HorizontalAlignment="Stretch"
          Margin="16"
          RowDefinitions="Auto, Auto">
      <StackPanel Grid.Row="0">
        <TextBox Text="" Watermark="Search"/>
        <ListBox/>
      </StackPanel>
      
      <StackPanel Grid.Row="1">
        <TextBox Text="{Binding FirstName}" Watermark="FirstName"/>
        <TextBox Text="{Binding MiddleName}" Watermark="MiddleName"/>
        <TextBox Text="{Binding SecondName}" Watermark="SecondName"/>
        <TextBox Text="{Binding Post}" Watermark="Post"/>
      </StackPanel>
    </Grid>
    
    <StackPanel Grid.Row="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
      <Button>
        <TextBlock Text="Save"/>
      </Button>
      <Button>
        <TextBlock Text="Cancel"/>
      </Button>
    </StackPanel>
  </Grid>
</UserControl>